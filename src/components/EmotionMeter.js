function emotionMeter() {
    const emotion = [feliz, triste];
    
    return ({emotion.map(emotion => (
        <li key={emotion}>{emotion}</li>
    )
    )})
}

<i class="fas fa-smile-beam"></i>
<i class="fas fa-frown"></i>